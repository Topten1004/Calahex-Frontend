import styled from "styled-components";

const TokenBaseInfoContainer = styled.div`

  min-height: calc(100vh);

  .text-right {
    text-align: right;
  }

  .token-info-header {
    padding: 15px 15px 0;
  }
`;

export default TokenBaseInfoContainer;